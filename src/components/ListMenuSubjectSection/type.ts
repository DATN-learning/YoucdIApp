export interface IIListMenuSubjectSectionProps{
    setIsShowPopupSubject: (isShow: boolean) => void;
}
export interface IItemMenuSubjectProps{
    title: string;
    bgColor: string;
    onPress: () => void;
}